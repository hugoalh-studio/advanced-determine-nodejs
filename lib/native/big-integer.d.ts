/**
 * @function isBigIntegerEven
 * @description Determine the big integer is even.
 * @param {bigint} item Item that need to determine.
 * @returns {boolean} Determine result.
 */
export function isBigIntegerEven(item: bigint): boolean;
/**
 * @function isBigIntegerIntegralNumericType
 * @description Determine the big integer is match the specified integral numeric type.
 * @param {string} typeName Name of the integral numeric type.
 * @param {bigint} item Item that need to determine.
 * @returns {boolean} Determine result.
 */
export function isBigIntegerIntegralNumericType(typeName: string, item: bigint): boolean;
/**
 * @function isBigIntegerNegative
 * @description Determine the big integer is negative.
 * @param {bigint} item Item that need to determine.
 * @returns {boolean} Determine result.
 */
export function isBigIntegerNegative(item: bigint): boolean;
/**
 * @function isBigIntegerOdd
 * @description Determine the big integer is odd.
 * @param {bigint} item Item that need to determine.
 * @returns {boolean} Determine result.
 */
export function isBigIntegerOdd(item: bigint): boolean;
/**
 * @function isBigIntegerPositive
 * @description Determine the big integer is positive.
 * @param {bigint} item Item that need to determine.
 * @returns {boolean} Determine result.
 */
export function isBigIntegerPositive(item: bigint): boolean;
/**
 * @function isBigIntegerPrime
 * @description Determine the big integer is prime.
 * @param {bigint} item Item that need to determine.
 * @returns {boolean} Determine result.
 */
export function isBigIntegerPrime(item: bigint): boolean;
/**
 * @function isBigIntegerSafe
 * @description Determine the big integer is safe with IEEE-754.
 * @param {bigint} item Item that need to determine.
 * @returns {boolean} Determine result.
 */
export function isBigIntegerSafe(item: bigint): boolean;
export { isBigIntegerEven as isBigIntEven, isBigIntegerIntegralNumericType as isBigIntIntegralNumericType, isBigIntegerNegative as isBigIntNegative, isBigIntegerOdd as isBigIntOdd, isBigIntegerPositive as isBigIntPositive, isBigIntegerPrime as isBigIntPrime, isBigIntegerSafe as isBigIntSafe };
//# sourceMappingURL=big-integer.d.ts.map